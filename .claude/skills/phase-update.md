Update the build phase tracker in `.claude/memory/build_phases.md`.

Mark the task(s) specified in the arguments as complete (change `- [ ]` to `- [x]`).

Then:
1. Show the updated task list for the current phase
2. State the next uncompleted task to work on
3. If all tasks in a phase are complete, announce the phase is done and show Phase N+1 tasks

Also update the status in `CLAUDE.md` build phase table if the overall phase status changes.